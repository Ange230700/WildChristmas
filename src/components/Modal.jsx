



const Modal = ({isModal}) => {


    return (
        <div className="containerModal"
        // {isModal === true ? "openModal" : "closeModal"}>
        >
        </div>
    );
};

export default Modal;